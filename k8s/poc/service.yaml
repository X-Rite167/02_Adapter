apiVersion: v1
kind: Service
metadata:
  name: confidential-adapter
  namespace: litellm
spec:
  type: ClusterIP
  selector:
    app: confidential-adapter
  ports:
    - name: http
      port: 8080
      targetPort: http
